{
    "name": "keboola/db-extractor-mysql",
    "license": "MIT",
    "type": "Application",
    "description": "Reads data from Mysql databases for keboola connection",
    "authors": [
        {
            "name": "Keboola",
            "email": "devel@keboola.com"
        }
    ],
    "require": {
        "keboola/db-extractor-common": "^9.4.0",
        "keboola/db-extractor-logger": "^1.0",
        "keboola/php-temp": "^1.0",
        "keboola/php-utils": "^2.3.1",
        "symfony/serializer": "^3.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^4.6",
        "keboola/coding-standard": "^2.0",
        "phpstan/phpstan": "^0.8"
    },
    "autoload": {
        "psr-4": {
            "Keboola\\": "src/Keboola/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Keboola\\DbExtractor\\Tests\\": "tests/Keboola/DbExtractor"
        }
    },
    "scripts": {
        "tests": "./wait-for-it.sh mysql:$MYSQL_DB_PORT -t 30 -- ./vendor/bin/phpunit",
        "phpstan": "phpstan analyse --no-progress --level=max ./src ./tests",
        "phpcs": "phpcs -n --ignore=vendor --extensions=php .",
        "phpcbf": "phpcbf -n --ignore=vendor --extensions=php .",
        "build": [
            "@phpcs",
            "@phpstan",
            "@tests"
        ],
        "ci": [
            "@composer validate --no-check-all --strict",
            "@build"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
