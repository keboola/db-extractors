{
    "name": "keboola/db-extractor-snowflake",
    "license": "MIT",
    "type": "project",
    "description": "Keboola Snowflake DB Extractor",
    "keywords": ["keboola", "snowflake", "db", "extractor"],
    "authors": [
        {
            "name": "Keboola",
            "email": "devel@keboola.com"
        }
    ],
    "require": {
        "php": ">=7.1",
        "keboola/db-extractor-common": "~9.1",
        "keboola/php-temp": "^1.0",
        "keboola/php-csv-db-import": "~2.3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "~5.0",
        "codeclimate/php-test-reporter": "^0.3",
        "keboola/coding-standard": "^6.0",
        "keboola/storage-api-client": "^4.14",
        "phpstan/phpstan-shim": "^0.9.2"
    },
    "autoload": {
        "psr-4": {
            "Keboola\\": "src/Keboola/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Keboola\\DbExtractor\\Tests\\": "tests/Keboola/DbExtractor/"
        }
    },
    "scripts": {
        "tests": "phpunit",
        "phpstan": "phpstan analyse --no-progress --level=max --debug ./src ./tests",
        "phpcs": "phpcs -n --ignore=vendor --extensions=php .",
        "phpcbf": "phpcbf -n --ignore=vendor --extensions=php .",
        "build": [
            "@phpcs",
            "@phpstan",
            "@tests"
        ],
        "ci": [
            "@composer validate --no-check-all --strict",
            "@build"
        ]
    }
}
