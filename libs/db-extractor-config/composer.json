{
    "name": "keboola/db-extractor-config",
    "description": "Config definition for database extractor",
    "type": "library",
    "license": "MIT",
    "require": {
        "symfony/config": "^3.0",
        "symfony/yaml": "^3.0"
    },
    "require-dev": {
        "keboola/coding-standard": "^2.0",
        "phpunit/phpunit": "^7.0",
        "phpstan/phpstan": "^0.8"
    },
    "autoload": {
        "psr-4": {
            "Keboola\\DbExtractorConfig\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Keboola\\DbExtractorConfig\\Tests\\": "tests/Keboola/DbExtractorConfig/"
        }
    },
    "scripts": {
        "tests": "./vendor/bin/phpunit",
        "phpstan": "phpstan analyse --no-progress --level=max ./src",
        "phpcs": "phpcs -n --ignore=vendor --extensions=php .",
        "phpcbf": "phpcbf -n --ignore=vendor --extensions=php .",
        "build": [
            "@phpcs",
            "@phpstan"
        ],
        "ci": [
            "@composer install",
            "@composer validate --no-check-all --strict",
            "@build"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
