parameters:
    db:
        host: localhost
        database: test
        user: travis
        password:
        port: 3306

    tables:
        -
            id: 0
            name: escaping
            query: "SELECT * FROM escaping"
            outputTable: in.c-main.escaping
            incremental: false
            primaryKey:
              -
                col1
            enabled: true
        -
            id: 1
            name: simple
            outputTable: in.c-main.simple
            incremental: false
            primaryKey: null
            table: escaping
            retries: 1
            columns:
              -
                col1
              -
                col2
